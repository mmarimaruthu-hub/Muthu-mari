# Issue Log

## 1. Overview
This document tracks all data, technical, and process issues identified during the development and execution of the Enterprise Patient 360 Data Platform ETL pipelines.

---

## 2. Issue Log Table

| Issue ID | Date Identified | Layer | Severity | Issue Description | Root Cause | Resolution / Workaround | Status | Date Resolved |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| ISS-001 | 2026-05-01 | Bronze | High | Column names in raw CSV files contained spaces, commas, brackets, and special characters which are invalid in Delta Lake column naming. | Source CSVs exported from EHR systems with raw, user-defined headers. | Implemented `clean_column_names()` function using `re.sub()` to replace all invalid characters with underscores and strip leading/trailing underscores. | Resolved | 2026-05-02 |
| ISS-002 | 2026-05-02 | Silver | High | `Total_Charges` and `Total_Costs` columns were ingested as strings with `$` and `,` formatting (e.g., `"$12,345.67"`), causing type-cast failures. | Source billing system exports financial data as formatted currency strings. | Applied `regexp_replace(col, r"[\$,]", "")` before casting to `DoubleType`. | Resolved | 2026-05-02 |
| ISS-003 | 2026-05-02 | Silver | Medium | `Length_of_Stay` contained the value `"120 +"` for patients with stays ≥ 120 days, causing `IntegerType` cast to return `null`. | Source system uses a string cap convention for long-stay patients. | Used `when(col == "120 +", 120).otherwise(col.cast(IntegerType()))` to handle this special case. | Resolved | 2026-05-03 |
| ISS-004 | 2026-05-03 | Silver | Medium | `Gender` column had mixed representations: both full words (`"Male"`, `"Female"`, `"Unknown"`) and abbreviations (`"M"`, `"F"`, `"U"`) were present across different file batches. | Inconsistency in data entry/export across different hospital facilities. | Applied standardization using `when/isin` logic to normalize all values to `M`, `F`, `U`. | Resolved | 2026-05-03 |
| ISS-005 | 2026-05-04 | Silver | Low | The column `Zip Code - 3 digits` was being renamed to `Zip_Code___3_digits` (triple underscores) after the generic `clean_column_names()` function ran. | The hyphen `-` surrounded by spaces became `_` and combined with adjacent spaces becoming `_`, creating triple underscores. | Added an explicit `withColumnRenamed("Zip_Code_-_3_digits", "Zip_Code_3_digits")` step after the generic cleaning in the Silver notebook. | Resolved | 2026-05-04 |
| ISS-006 | 2026-05-05 | Gold | High | Window function calculations for `cumulative_length_of_stay` and `lifetime_total_charges` were incorrect when only the incremental batch was passed to the Gold notebook instead of full patient history. | Cumulative/window functions require all historical records for a patient to compute correctly. | Redesigned the incremental Gold strategy to: (1) identify affected patients, (2) pull ALL historical Silver rows for those patients, then (3) recompute full Patient 360 and MERGE. | Resolved | 2026-05-07 |
| ISS-007 | 2026-05-06 | Gold | Medium | `CCS_Diagnosis_Code`, `CCS_Procedure_Code`, `APR_DRG_Code`, and `APR_Severity_of_Illness_Code` remained as `StringType` in Silver, causing `IntegerType` casts in Gold to produce unexpected nulls for non-numeric entries. | Some code values in source data contained descriptions or `"NaN"` strings. | Added explicit `.cast("int")` in the Gold pipeline with null tolerance; downstream aggregations use `countDistinct` which ignores nulls. | Resolved | 2026-05-07 |
| ISS-008 | 2026-05-08 | Bronze | Low | Auto Loader checkpoint directory not found on first run in a fresh environment, causing an informational error message in the cleanup cell. | Expected behavior — checkpoint does not exist until first successful run. | Wrapped checkpoint cleanup in `try/except` block; error is caught and logged as an informational message, not a failure. | Resolved | 2026-05-08 |
| ISS-009 | 2026-05-10 | Silver | Medium | Watermark-based incremental filter returned 0 rows on re-run even when new Bronze data was available, because Silver watermark was compared against Silver timestamp, not Bronze timestamp. | Incorrect comparison logic in the watermark filter — should compare `bronze_load_timestamp` against `silver_load_timestamp` watermark. | Corrected filter to: `df_bronze.filter(col("bronze_load_timestamp") > lit(old_watermark))`. | Resolved | 2026-05-10 |
| ISS-010 | 2026-05-12 | Gold | Low | `OPTIMIZE` command on Gold table timed out in very large first-run scenarios (500M+ records). | Large initial data volume combined with default cluster configuration. | Scheduled `OPTIMIZE` to run on a larger driver/worker configuration. Added `autoCompact` and `optimizeWrite` options to the initial write to reduce file fragmentation. | Mitigated | 2026-05-13 |
